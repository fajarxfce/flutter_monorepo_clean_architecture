name: auth_data
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../../core/shared
  network:
    path: ../../core/network
  auth_domain:
    path: ../../domains/auth

dev_dependencies:
  build_runner:
  json_serializable:
  retrofit_generator:
  injectable_generator:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
