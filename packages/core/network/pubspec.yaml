name: network
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  shared:
    path: ../shared

dev_dependencies:
  build_runner:
  injectable_generator:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
